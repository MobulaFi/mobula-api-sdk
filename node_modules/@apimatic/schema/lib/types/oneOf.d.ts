import { Schema } from '../schema';
declare type SchemaType<T extends Schema<any, any>> = T extends Schema<infer U, any> ? U : never;
declare type ArraySchemaType<T extends Array<Schema<any, any>>> = T[number] extends Schema<any, any> ? SchemaType<T[number]> : never;
declare type DiscriminatorMap<T extends Array<Schema<any, any>>> = {
    [K in ArraySchemaType<T>]?: Schema<ArraySchemaType<T>>;
};
export declare function oneOf<T extends Array<Schema<any, any>>>(schemas: [...T], discriminatorMap?: DiscriminatorMap<T>, discriminatorField?: string): Schema<ArraySchemaType<T>>;
export {};
//# sourceMappingURL=oneOf.d.ts.map